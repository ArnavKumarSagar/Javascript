// arrray in js are resizable 
// arrays use shallow copies (direct points to actual referene point)
const arr=[1,2,4,45,6];
console.log(arr[1]);

/* array methods 
    push->adds at last
    pop->removes last element 
    unshift->adds at first   
    shift->removes from start
    join->converts array to strings
    slice(s,e)->it separate the stirng from s to e and not includes e index term ,also it does not change anything in main array
    splice(s,e)->it separates the string from s to e and includes e index term ,also it changes main array by removing the elements from s to e 

*/

